# CM HASH Frontend

Next.js + React + TypeScript + TailwindCSS + Shadcn UI demo app.

## Setup

1. `cd frontend`
2. `npm install`
3. `npm run dev`

## Environment

Copy `.env.example` to `.env.local`.

## Docker

Build and run the frontend container:

```sh
cd ..
docker-compose up --build frontend
```
