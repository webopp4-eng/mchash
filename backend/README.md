# CM HASH Backend

Express + TypeScript API for CM HASH.

## Setup

1. `cd backend`
2. `npm install`
3. `npm run dev`

## Environment

Copy `.env.example` to `.env`.

## Docker

Build and run the backend container:

```sh
cd ..
docker-compose up --build backend
```
